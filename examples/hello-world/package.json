{
  "name": "hello-world",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "volt dev",
    "build": "volt build",
    "preview": "volt preview",
    "lint": "eslint . --max-warnings 0",
    "test": "tsc --noEmit",
    "package": "volt package",
    "doctor": "volt doctor"
  },
  "dependencies": {
    "voltkit": "workspace:*",
    "@voltkit/volt-cli": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
