{
  "name": "todo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "volt dev",
    "build": "volt build",
    "lint": "eslint . --max-warnings 0",
    "test": "pnpm exec vitest run"
  },
  "dependencies": {
    "voltkit": "workspace:*",
    "@voltkit/volt-cli": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
