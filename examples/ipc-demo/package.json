{
  "name": "ipc-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "volt dev",
    "build": "volt build",
    "preview": "volt preview",
    "lint": "eslint . --max-warnings 0",
    "test": "pnpm exec vitest run",
    "verify:secure-storage:manual": "node ./scripts/manual-secure-storage-checklist.mjs"
  },
  "dependencies": {
    "voltkit": "workspace:*",
    "@voltkit/volt-cli": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
